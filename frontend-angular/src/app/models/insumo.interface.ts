export interface Insumo { 
    nombre: string; // <-- ESTA LÍNEA ES LA QUE PROBABLEMENTE FALTA O ESTÁ MAL ESCRITA
    stock: number;
    unidadMedida: string; // La estás usando en tu HTML, así que debe existir
    diasRestantes: number;
}
//añadir todas las propiedades para que funcione el back
